# Database migrations

M2 introduces reversible PostgreSQL migrations here after the tenant model and row-level security tests are implemented. The planning scaffold intentionally has no business schema.
